import { createClient } from 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.112.3/+esm';
import { SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY } from './dashboard-config.js';
const supabase=createClient(SUPABASE_URL,SUPABASE_PUBLISHABLE_KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
const fmt=n=>new Intl.NumberFormat('en-GB').format(Number(n||0));
const mins=s=>fmt(Math.round(Number(s||0)/60));
const iconFor=a=>({hug:'♡',kiss:'✦',bite:'◇',cuddle:'∞',headpat:'⌁',pat:'⌁',poke:'•',slap:'⚡',snuggle:'☾',tickle:'✧',handhold:'⌘',fistbump:'◆',bonk:'◈'}[a]||'♡');
const label=a=>String(a||'').replaceAll('_',' ').replace(/\b\w/g,m=>m.toUpperCase());
let data=null,loading=null;
async function load(){if(data)return data;if(loading)return loading;loading=(async()=>{const{data:{session}}=await supabase.auth.getSession();if(!session?.access_token)throw new Error('Sign in required.');const r=await fetch('/api/profile',{headers:{Authorization:`Bearer ${session.access_token}`}});const d=await r.json();if(!r.ok)throw new Error(d.error||'Could not load Bound profile.');data=d;return d})().finally(()=>loading=null);return loading;}
function avatar(user,size='lg'){return user?.avatar_url?`<img class="show-avatar ${size}" src="${esc(user.avatar_url)}" alt="${esc(user.display_name)}">`:`<span class="show-avatar ${size} fallback">${esc((user?.display_name||'B')[0])}</span>`}
function personAvatar(url,name){return url?`<img src="${esc(url)}" alt="${esc(name)}">`:`<span>${esc((name||'?')[0])}</span>`}
function stat(icon,kicker,value,copy){return `<article><span>${icon}</span><small>${kicker}</small><b>${value}</b><p>${copy}</p></article>`}
function relationshipCard(r){return `<article class="bond-card"><div class="bond-person">${personAvatar(r.other_avatar,r.other_name)}<div><small>${r.role==='owner'?'YOUR SUB':'YOUR OWNER'}</small><b>${esc(r.other_name)}</b><span>${esc(r.relationship_name||'Bound connection')}</span></div></div><div class="bond-meter"><div><b>Bond level ${fmt(r.bond_level)}</b><span>${fmt(r.bond_xp)} XP</span></div><i><em style="width:${Math.min(100,(Number(r.bond_xp||0)%1000)/10)}%"></em></i></div><div class="bond-stats"><span><b>${fmt(r.interactions)}</b><small>interactions</small></span><span><b>${fmt(r.gag_count)}</b><small>gags</small></span><span><b>${r.currently_gagged?'Active':'Clear'}</b><small>gag state</small></span></div></article>`}
function allRpRows(actions){if(!actions?.length)return '<div class="show-empty">No RP stats yet. Use Bound social commands and they will appear here automatically.</div>';return `<div class="all-stats-list">${actions.map(a=>`<div class="all-stat-row"><span class="all-stat-icon">${iconFor(a.action)}</span><b>/${esc(a.action)}</b><span>${fmt(a.given)} sent</span><span>${fmt(a.received)} received</span><strong>${fmt(a.total)}</strong></div>`).join('')}</div>`}
function profileHTML(d){const u=d.user,g=d.gag_stats||{},rels=d.relationships||[],active=d.active_gag,actions=d.rp_actions||[];const totalRp=d.totals.rp_given+d.totals.rp_received;return `<div class="showcase-shell"><section class="profile-hero" ${u.banner_url?`style="--hero-banner:url('${esc(u.banner_url)}')"`:''}><div class="profile-glow"></div><div class="profile-ident">${avatar(u)}<div><span class="show-kicker">BOUND IDENTITY</span><h2>${esc(u.display_name)}</h2><p>${esc(u.custom_title||u.role_preference||'Bound member')}</p><div class="profile-tags"><span>${esc(u.subscription_tier||'Standard')}</span><span>Level ${fmt(u.bdsm_level)}</span>${u.looking_for_owner?'<span>Seeking owner</span>':''}${u.looking_for_sub?'<span>Seeking sub</span>':''}<span>${u.profile_visible?'Visible profile':'Private profile'}</span></div></div></div><div class="profile-bio"><small>ABOUT</small><p>${esc(u.bio||'Your Bound identity lives here — relationships, bond progress, social history and consent-aware roleplay.')}</p></div></section><div class="show-stat-grid">${stat('♡','CONNECTIONS',fmt(d.totals.relationships),`${fmt(d.totals.owners)} owners · ${fmt(d.totals.subs)} subs`)}${stat('✦','BDSM XP',fmt(u.bdsm_lifetime_xp),`${fmt(u.bdsm_xp)} current XP`)}${stat('◉','TIMES GAGGED',fmt(g.total_times_gagged),`${fmt(g.total_messages_converted)} messages converted`)}${stat('∞','RP INTERACTIONS',fmt(totalRp),`${fmt(d.totals.rp_given)} sent · ${fmt(d.totals.rp_received)} received`)}</div><section class="show-section"><div class="show-heading"><div><span class="show-kicker">RELATIONSHIPS</span><h3>Your Bound connections</h3></div><span class="privacy-chip">${u.profile_visible?'Profile visible':'Profile private'}</span></div><div class="bond-grid">${rels.length?rels.map(relationshipCard).join(''):'<div class="show-empty">No active ownership relationships yet. Your accepted claims will appear here automatically.</div>'}</div></section><section class="profile-foot-grid"><article class="show-panel"><span class="show-kicker">GAG HISTORY</span><h3>Your gag stats</h3><div class="feature-lines"><div><span>Total times gagged</span><b>${fmt(g.total_times_gagged)}</b></div><div><span>Total gag time</span><b>${mins(g.total_gag_seconds)} min</b></div><div><span>Longest gag</span><b>${mins(g.longest_gag_seconds)} min</b></div><div><span>Messages converted</span><b>${fmt(g.total_messages_converted)}</b></div><div><span>Reveals</span><b>${fmt(g.total_reveals)}</b></div><div><span>Ungagged</span><b>${fmt(g.total_times_ungagged)}</b></div><div><span>Current state</span><b>${active?`Gagged · ${esc(active.gag_style||'active')}`:'Clear'}</b></div></div></article><article class="show-panel consent-panel"><span class="show-kicker">CONSENT & PRIVACY</span><h3>Your boundaries and your data</h3><p>Ownership and gag interactions are consent-aware and revocable. For a full Bound data deletion request, open Discord and use <b>/profile</b>, then choose the data deletion option. The request is routed into Bound’s protected safety review flow.</p><div class="consent-badges"><span>Relationship aware</span><span>Revocable</span><span>Auditable</span><span>Deletion requests protected</span></div><a class="show-cta" href="https://discord.gg/NVseqMDNRd" target="_blank" rel="noopener noreferrer">Open Bound Society →</a></article></section><section class="show-section"><div class="show-heading"><div><span class="show-kicker">ALL SOCIAL STATS</span><h3>Everything Bound remembers</h3></div><span class="privacy-chip">${fmt(totalRp)} total RP</span></div>${allRpRows(actions)}</section></div>`}
function rpHTML(d){const u=d.user,actions=d.rp_actions||[],top=actions.slice(0,8),rels=d.relationships||[],total=d.totals.rp_given+d.totals.rp_received;return `<div class="showcase-shell"><section class="rp-hero"><div><span class="show-kicker">SOCIAL & ROLEPLAY</span><h2>Discord interactions,<br><em>with a pulse.</em></h2><p>Bound turns simple commands into a social layer — interactive responses, mutual stats, ownership context and a profile that remembers how you connect.</p><div class="rp-command-row"><span>/hug</span><span>/kiss</span><span>/bite</span><span>/cuddle</span><span>/headpat</span><span>/poke</span><span>/slap</span></div></div><div class="rp-orbit"><div class="rp-core">♡</div><i class="o1">✦</i><i class="o2">∞</i><i class="o3">◇</i></div></section><div class="show-stat-grid">${stat('↗','SENT',fmt(d.totals.rp_given),'roleplay interactions')}${stat('↙','RECEIVED',fmt(d.totals.rp_received),'from other users')}${stat('♡','BOUND WITH',fmt(rels.length),'active connections')}${stat('◉','TOP ACTION',esc(top[0]?.action||'—'),`${fmt(top[0]?.total||0)} combined`)}</div><section class="show-section"><div class="show-heading"><div><span class="show-kicker">YOUR SOCIAL SIGNATURE</span><h3>Most-used interactions</h3></div><span class="privacy-chip">${fmt(total)} total</span></div><div class="action-grid">${top.length?top.map((a,i)=>`<article class="action-card"><span class="action-rank">0${i+1}</span><div class="action-icon">${iconFor(a.action)}</div><h4>/${esc(a.action)}</h4><div class="action-count">${fmt(a.total)}</div><p>${fmt(a.given)} sent · ${fmt(a.received)} received</p><i><em style="width:${Math.max(8,Math.round((a.total/(top[0]?.total||1))*100))}%"></em></i></article>`).join(''):'<div class="show-empty">Use Bound RP commands and your interaction signature will build itself here.</div>'}</div></section><section class="show-section"><div class="show-heading"><div><span class="show-kicker">ALL INTERACTION STATS</span><h3>Every action, sent and received</h3></div></div>${allRpRows(actions)}</section><section class="profile-foot-grid"><article class="show-panel"><span class="show-kicker">WHY IT FEELS DIFFERENT</span><h3>Interactions remember you</h3><div class="feature-lines"><div><span>Mutual interaction tracking</span><b>Live</b></div><div><span>Ownership-aware actions</span><b>Live</b></div><div><span>Interactive response buttons</span><b>Live</b></div><div><span>Curated action media</span><b>Live</b></div><div><span>Profile stat history</span><b>Live</b></div></div></article><article class="show-panel social-callout"><span class="show-kicker">BOUND TOGETHER</span><h3>More than command spam.</h3><p>Every interaction contributes to a recognisable social identity. Your profile, bonds, gag history and RP activity all connect into the same Bound experience.</p><button data-show-profile>View my Bound profile →</button></article></section></div>`}
async function render(key){const el=document.getElementById(`view-${key}`);if(!el)return;el.dataset.showcase='1';el.innerHTML='<div class="show-loader"><i></i><b>Loading your Bound world…</b></div>';try{const d=await load();el.innerHTML=key==='profile'?profileHTML(d):rpHTML(d);el.querySelector('[data-show-profile]')?.addEventListener('click',()=>document.querySelector('.nav-item[data-view="profile"]')?.click())}catch(e){el.innerHTML=`<div class="show-empty big"><b>Couldn’t load this part of your profile.</b><span>${esc(e.message)}</span></div>`}}
function arm(){document.querySelectorAll('.nav-item[data-view="profile"],.nav-item[data-view="roleplay"]').forEach(btn=>btn.addEventListener('click',()=>setTimeout(()=>render(btn.dataset.view),0)));document.querySelectorAll('[data-jump="roleplay"]').forEach(btn=>btn.addEventListener('click',()=>setTimeout(()=>render('roleplay'),0)))}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',arm);else arm();
